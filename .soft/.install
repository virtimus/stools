_dFolder

echo "[stools/.install] starting ... $dFolder"

if [ ! -e $sPath/src/stools ]; then
    echo "[stools/.install] $sPath/src/stools ..." 
    ln -s $sPath/.downloads/$dFolder/stools $sPath/src/stools
fi

echo "[stools/.install] end."